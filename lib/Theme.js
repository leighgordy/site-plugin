/**
 * This is the main theme used by the plugin. It is written to override/inherit from the main sites
 * theme.
 * @param parentTheme - The parent theme inherited from the main site outside the plugin.
 */
export default (parentTheme) => ({
  ...parentTheme,
});
